name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'
          cache: true
          cache-dependency-path: '**/*.csproj'

      - name: Restore
        run: |
          dotnet restore GitReport.slnx
          dotnet restore GitReport/GitReport.csproj -r win-x64

      - name: Build
        run: dotnet build GitReport.slnx --no-restore -c Release

      - name: Test
        run: dotnet test GitReport.slnx --no-restore --no-build -c Release

      - name: Publish (win-x64 self-contained)
        run: dotnet publish GitReport/GitReport.csproj -r win-x64 --self-contained -c Release --no-restore -o publish/

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: GitReport-win-x64
          path: publish/
          if-no-files-found: error
